<div class="container">





<div class="left">


        <h1> Uniform</h1>
        <h3> L'inscription devient plus simple</h3>

        <ul>
          <li> Un seul formulaire pour toutes les écoles</li>
          <li>Gérez facilement vos candidatures</li>
          <li> Stockez vos données scolaires</li>


        </ul>


</div>

<div class="right">


<div class="logg">
<h2>Log in</h2>
</div>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>


  <div class="field mail">
  
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "emaild" %>
  </div>

</br>

  <div class="field password">
    
    <%= f.password_field :password, autocomplete: "off", class: "emaild" %>
  </div>

</br>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Log in", class: "buta" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>
</div>
